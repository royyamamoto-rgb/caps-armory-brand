name: release
on:
  workflow_dispatch:
    inputs:
      dist_tag:
        description: "npm dist-tag (alpha|beta|latest)"
        required: true
        default: "alpha"
permissions:
  contents: write
  id-token: write
concurrency:
  group: release
  cancel-in-progress: false
jobs:
  publish:
    runs-on: ubuntu-24.04
    environment: release
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 9.12.3
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
          registry-url: "https://registry.npmjs.org"
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - name: Pre-publish gates
        run: |
          pnpm test
          pnpm test:tarball
          pnpm drift-check
        env:
          THEMES_READ_PAT: ${{ secrets.THEMES_READ_PAT }}

      - name: Enforce alpha dist-tag for 0.x prereleases
        run: |
          version=$(node -p "require('./package.json').version")
          if [[ "$version" == *-alpha* && "${{ inputs.dist_tag }}" != "alpha" ]]; then
            echo "Version $version is alpha but dist_tag is ${{ inputs.dist_tag }}; refusing."
            exit 1
          fi

      - name: changesets publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          # Auth via NPM_TOKEN (lumina28 granular publish token, expires 2026-07-03).
          # Follow-up: rotate to OIDC trusted-publisher + restore --provenance for 0.0.1-alpha.1.
          pnpm changeset publish --no-git-tag
